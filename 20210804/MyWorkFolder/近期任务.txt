OK
1、按照最新版本部署，mysql数据库5.7.30，最新数据库文件是解压压缩包后，sql文件夹里ry-uflo.sql文件，新建数据库时字符集设置utf8mb4，排序规则utf8mb4_general_ci 参考图片  管理员最新登录密码jzjt3202
============================================================
OK
2、站内信箱问题：最好区分一下收件箱和发件箱，不要混在一起，可以用选项卡的方式区分，写消息时的接收人要有人员选择页，参考招聘申请里面的申请人的填写，
‘已读’字段应该改成‘状态’字段，包含已读、未读，日期应拆分为发件时间、读取时间。
信箱增加添加附件功能，可以参考日常办公里面档案信息中的附件上传功能。

（群发跟附件同时实现，在不存储多个文件的情况下有2解决方案：
1，数据库条目存多条，每条对应到一个人，附件库的文件名都对应到同一个文件。
2，数据库条目存单条，条目接收人是长字符串，人名用逗号分割，附件库一对一
两种方法比较，长字符串很明显受数据库词条长度限制，最多可能就一两百个人名，虽然1方法在群发的时候会在数据库里同时增加许多条目，产生一些冗余但是容量肯定比一两百个要大，故采用方法1
PS 如果真的要优化，也是有办法。发信往往会发给部门或者总部门下面所有的人，此时使用线段树+懒更新的算法效果最佳，存储时间复杂度由O(n）降到O(lgn)，读取时间由O(1)升到O(lgn)，不计算数据库效率的情况下）
(改了一个数据库条目，但是应该没影响，顺便我用的您的代码的人员选择页运行有点奇怪，添加的时候会把之前添加的人取消掉）
（hrm-js.js也更新了几条函数）
==================================================================
OK
3、员工统计名字改为员工信息，照片上传有BUG，员工合同不能新增？
员工调动、员工考勤功能暂时屏蔽不要。
员工合同功能应该跟员工信息关联合并，员工信息应该跟我做的员工档案关联（员工档案信息是钉钉同步来的），
(修改了employee表，取消了login_id的限制,删除索引）
（关于员工合同，计划采用attachment.js进行管理，不需要在员工信息中新增字段。新增一个下载按钮以供下载，或者直接使用fileinput提供的预览下载功能）
（填写员工信息时读取钉钉数据库来自动填写，这一步有些麻烦。因为钉钉员工会很多，弹出列表来选择可能不是便捷，也不方便导入大量数据，所以可以考虑在表中填写一个钉钉id来定位员工，在单个导入和批量导入应该都是可以操作的，只需要从钉钉数据库里导出人员id）
（我觉得是需要两张表？或者是动态表？可以用动态表比较好一点，但是表单校验条件怎么办？如果是填表，姓名不能为空，如果是导入，钉钉id不能为空，这种情况下如何表单校验？动态表添加require属性试下
解决方法是，姓名为主，只对姓名进行校验）
（初步计划：add页面给一个两个按钮，或者在钉钉ID后面跟一个导入按钮，导到后台找到钉钉员工数据
如果是前者，就打开新add界面，从modelview塞进去查到的数值
如果是后者，就把数值穿回来，前端函数填写进表
采用了后者的方法，这种方式稍加修改可以作为批量导入的代码）

(可以直接用operate.save(),写一个callback函数填表）
填写员工信息时可以选取员工档案已有信息，并把相关已有信息填充进来不用重复填写了，跟系统登录用户也做关联，但不是强关联，
(我已经把员工信息的require去掉了）
因为有员工没有系统登陆账户。
(第一个，数据库里直接改了名字，并且删除了调动，考勤和员工合同的数据库条目)
(因为我是Windows系统，修改了application.yml中的profile路径，头像存储要用到。员工合同之前是严格绑定员工账户所以不能主动新增。我会调整的）
(感觉不改好像也能传？hrm_message的信息就传上去了)
(这一步需要把员工合同跟员工信息解绑，再把员工信息和员工档案松绑定，有本地员工档案的都有钉钉员工档案，有钉钉员工档案的不一定有本地员工档案，本地员工∈钉钉员工)
(但是也不一定，所以不用严格绑定，搞不好用户账户会易主之类的。所以采取把本地用户id绑到钉钉员工id上的方式并不用很严格）
（TODO:	最后，我的人事模块也需要相应修改，这里做个备忘）（已做）
=====================================================================
4、在人资管理大模块下新增 ‘薪资保险’和‘绩效考核’两个模块，分别对应我给你发的文档中的功能进行开发，参考图片


    不清楚的地方随时问我。

			问题：

我有一个问题就是删除的时候怎么办，一直没有写删除的代码
如果要写删除的代码绕不开修改fileinput的参数，得到attacment.js中修改如loadEditFiles()这种函数，我没有动
另外，员工统计里好像还是有bug，但我去测试的时候又找不到了
有一些不用的功能没有清理的，需要清理掉吗？